[package]
name = "main_backend"
version = "0.1.0"
edition = "2021"

[dependencies]
libloading = "0.8.5"
serde = { version = "1.0.204", features = ["derive"] }
serde_json = "1.0.120"
ws_server = { version = "0.1.0", path = "../ws_server" }
